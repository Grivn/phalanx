syntax = "proto3";

package protos;

import "github.com/ultramesh/flato-common/types/protos/transaction.proto";

message transaction {
  protos.Transaction tx = 1;
}

// transaction set is used to transfer the txs
message transaction_set {
  string batch_hash = 1;
  repeated transaction tx_list = 2;
}